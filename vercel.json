{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run migrate && bun run build",
  "regions": ["hnd1"],
  "crons": [
    {
      "path": "/api/audit-packs/sweep",
      "schedule": "0 3 * * *"
    }
  ]
}
